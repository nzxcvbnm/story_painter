const dx_ = 'a';

const dy_ = 'b';

const timestamp_ = 'c';

const cpStart_ = 'e';

const cpEnd_ = 'f';

const end_ = 'g';

const startSize_ = 'h';

const endSize_ = 'j';

const location_ = 'k';

const size_ = 'l';

const id_ = 'm';

const points_ = 'n';

const lines_ = 'o';

const arcs_ = 'p';

const threshold_ = 'r';

const smoothRatio_ = 's';

const color_ = 't';

const width_ = 'u';

const type_ = 'w';

const painterDrawTypeArc = 'x';

const painterDrawTypeLine = 'y';

const painterDrawTypeShape = 'z';

const start_ = 'd';
